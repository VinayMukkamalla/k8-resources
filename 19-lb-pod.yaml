apiVersion: v1
kind: Pod
metadata:
  name: loadbalancer-demo
  # These labels are used to register the pod to any service with same selector labels
  labels:
    project: roboshop
    component: frontend
    environment: dev
    purpose: loadbalancer
spec:
  containers:
  - name: nginx
    image: nginx