apiVersion: v1
kind: pod

metadata:
  name: nginx
  namespace: roboshop
  labels:
    environment: development
    project: roboshop
spec:
  containers:
  - name: nginx
    image: nginx
    ports:
    - containerport: 80

